# System Layer (逻辑系统层)

## 核心职责
1. **纯逻辑计算**: 存放游戏的核心算法（如网格算法、合成配方判定、数值计算公式）。
2. **逻辑一致性**: 提供可以被前后端复用的计算逻辑。

## 核心原则
1. **无状态 (Stateless)**: 尽量设计为输入 A 得到 B，不依赖全局变量。
2. **脱离 Unity**: 严禁依赖 `UnityEngine` 命名空间，确保 100% 可单元测试。
